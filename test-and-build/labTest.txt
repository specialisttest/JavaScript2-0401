1. Написать модуль CommonJS экспортировав ф-ю
gcd(a, b) {
    let c;
    while ( (c = a % b) != 0) {
        a = b; b = c;
    }
    return b;
}

2. Реализовать тест (jest) который проверит gcd на разных хначениях
(например gcd(1071, 462) == 21)